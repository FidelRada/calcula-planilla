<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.0</version>
		<relativePath/>
	</parent>

	<groupId>bo.diplomado</groupId>
	<artifactId>calcula-api</artifactId>
	<version>1.1.0</version>
	<name>calcula-api</name>
	<description>API de liquidacion de planilla - Proyecto Final Modulo 4 CI/CD</description>

	<properties>
		<java.version>21</java.version>
		<jacoco.version>0.8.13</jacoco.version>
		<!-- Quality Gate: cobertura minima exigida por el pipeline.
		     Vive parametrizada para poder moverla sin tocar el workflow. -->
		<jacoco.cobertura.minima>0.80</jacoco.cobertura.minima>
	</properties>

	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>

		<!-- Actuator: /actuator/health para el health check del deployment -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>

			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<version>${jacoco.version}</version>
				<configuration>
					<excludes>
						<!-- Solo contiene el main() de Spring Boot -->
						<exclude>**/CalculaApplication.class</exclude>
					</excludes>
					<!-- Las reglas se declaran a nivel de plugin (no dentro de una
					     ejecucion) para que tambien apliquen cuando el pipeline
					     invoca la meta suelta `jacoco:check`. -->
					<rules>
						<rule>
							<element>BUNDLE</element>
							<limits>
								<limit>
									<counter>LINE</counter>
									<value>COVEREDRATIO</value>
									<minimum>${jacoco.cobertura.minima}</minimum>
								</limit>
								<!-- Este es el que importa en este proyecto: PlanillaService
								     tiene once ramas y es donde un caso sin prueba se nota. -->
								<limit>
									<counter>BRANCH</counter>
									<value>COVEREDRATIO</value>
									<minimum>${jacoco.cobertura.minima}</minimum>
								</limit>
							</limits>
						</rule>
					</rules>
				</configuration>
				<executions>
					<execution>
						<id>prepare-agent</id>
						<goals><goal>prepare-agent</goal></goals>
					</execution>
					<execution>
						<id>report</id>
						<phase>test</phase>
						<goals><goal>report</goal></goals>
					</execution>
					<execution>
						<id>check</id>
						<phase>verify</phase>
						<goals><goal>check</goal></goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
</project>
